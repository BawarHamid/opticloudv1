// This 'loading' file ensures that the navbar is still shown for all routes in this layer while loading is active

export default function Loading() {
    return (
        <main>
            <h1>Loading...</h1>
        </main>
    );
}
